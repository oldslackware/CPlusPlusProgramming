// Questo programma, dato un quantitativo n di numeri definiti dall'utente,
// ne calcola la media matematica e la media geometrica.

#pragma hdrstop
#include <condefs.h>
#include <iostream.h>
#include <ctype.h>
#include <math.h>
//---------------------------------------------------------------------------
#pragma argsused

int num,        //definisce il quantitativo di numeri in input.
    cont;       //conta il numero di input effettuati.
float numero,   //riceve il numero in input.
      somma,    //contiene la somma di tutti i numeri in input
      moltipl;  //contiene il prodotto di tutti i numeri in input
int main(int argc, char* argv[])
{
  num =0
  while (num < 2)
  {
  cout << "Numero di dati in input  = ";
  cin  >> num;
  }


// fine con return
cout <<"\n\n\n\tPremere return per terminare";
cin.ignore (4, '\n');
cin.get();
}

