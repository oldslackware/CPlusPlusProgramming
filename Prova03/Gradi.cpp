/* Gradi.cpp
 * Trasforma un radiante in gradi primi e secondi
 */

#pragma hdrstop
#include <condefs.h>
#include <iostream.h>
#include <math.h>

//---------------------------------------------------------------------------
#pragma argsused
int main() {
   float radianti,      //Valore in input
         appoggio,      //variabile di appoggio dei dati
         gradi,         //Valore in gradi
         primi,         //valore in primi
         secondi;       //valore in secondi

  //cout << M_PI << endl;
  cout << "Dato un angolo in radianti lo trasforma in gradi, primi, secondi." << endl
        << "Angolo (in radianti):   ";
  cin  >> radianti;

 appoggio = 

   //area = raggio * raggio * 3.14;
   //circon = raggio * 6.28;
   //cout << raggio << " * " << raggio << " * 3,14 = " << area << endl;
   //cout << raggio << " * 6,28 = " << circon << endl;


  cout << "\n\n\tPremere return per terminare";
  cin.ignore(4, '\n');
  cin.get();
}

